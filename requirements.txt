# Runtime dependencies for fact_checker
# All deps are declared in pyproject.toml.
# This file exists as a fallback for tools that expect requirements.txt.
# Install with: pip install -e ".[dev]"
# Or directly: pip install -r requirements.txt

# Core LLM
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# Settings & validation
pydantic>=2.8.0
pydantic-settings>=2.3.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Web / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# CLI & TUI
typer>=0.12.3
rich>=13.7.0
textual>=0.80.0

# Transcript / ASR
youtube-transcript-api>=0.6.0
faster-whisper>=1.0.0
yt-dlp>=2024.1.1

# Utilities
python-dotenv>=1.0.1

# Dev / test (optional but needed for CI)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-anyio>=0.0.0
anyio[trio]>=4.0.0
ruff>=0.4.0
mypy>=1.10.0
